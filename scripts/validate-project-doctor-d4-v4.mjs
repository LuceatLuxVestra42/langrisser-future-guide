import assert from 'node:assert/strict';
import fs from 'node:fs';
import { executePlan } from './run-project-doctor-d4.mjs';
const d3=JSON.parse(fs.readFileSync('data/contracts/project-doctor-d3-validator-plan.v4.json','utf8'));const d4=JSON.parse(fs.readFileSync('data/contracts/project-doctor-d4-execution.v4.json','utf8'));const by=id=>d3.checkCatalog.find(x=>x.id===id);const plan=(status,ids=[],manualReviews=[])=>({version:4,stage:'D3',status,changedFileCount:1,selectedChecks:ids.map(id=>({...by(id),execution:'PLANNED'})),manualReviews});const done=[];const run=(id,fn)=>{fn();done.push(id)};
run('HERO_ARTWORK_OWNER_EXECUTES',()=>{const seen=[];const r=executePlan({plan:plan('PLAN_READY',['hero-artwork-final','production-build']),d3Contract:d3,d4Contract:d4,executor:x=>{seen.push(x.id);return{status:0}}});assert.equal(r.status,'PASS_EXECUTED');assert.deepEqual(seen,['hero-artwork-final','production-build'])});
run('HERO_ARTWORK_FAIL_FAST',()=>{const seen=[];const r=executePlan({plan:plan('PLAN_READY',['hero-artwork-final','localization-audit','production-build']),d3Contract:d3,d4Contract:d4,executor:x=>{seen.push(x.id);return{status:x.id==='hero-artwork-final'?7:0}}});assert.equal(r.status,'FAIL_CHECK');assert.equal(r.failedCheckId,'hero-artwork-final');assert.deepEqual(seen,['hero-artwork-final'])});
run('REMAINING_MANUAL_PRESERVED',()=>{const r=executePlan({plan:plan('MANUAL_REVIEW',['production-build'],[{node:'skin-assets'}]),d3Contract:d3,d4Contract:d4,executor:()=>({status:0})});assert.equal(r.status,'REVIEW_MANUAL');assert.equal(r.exitCode,3)});
run('STRICT_COMMAND',()=>{const p=plan('PLAN_READY',['hero-artwork-final']);p.selectedChecks[0].command='node scripts/validate-hero-artwork-final.mjs';const r=executePlan({plan:p,d3Contract:d3,d4Contract:d4,executor:()=>({status:0})});assert.equal(r.status,'INVALID_PLAN')});
run('DRY_RUN',()=>{let calls=0;const r=executePlan({plan:plan('PLAN_READY',['hero-artwork-final']),d3Contract:d3,d4Contract:d4,dryRun:true,executor:()=>{calls++;return{status:0}}});assert.equal(r.status,'PASS_DRY_RUN');assert.equal(calls,0)});
assert.equal(d4.schemaId,'project-doctor-d4-execution/v4');assert.equal(d4.status,'DESIGN_FROZEN');assert.deepEqual([...new Set(d4.allowedCheckIds)].sort(),d3.checkCatalog.map(x=>x.id).sort());console.log(JSON.stringify({status:'PASS_PROJECT_DOCTOR_D4_V4_EXECUTION_FIXTURES',fixturePassCount:done.length,fixtureCount:done.length,fixtures:done,allowedCheckCount:d4.allowedCheckIds.length},null,2));
