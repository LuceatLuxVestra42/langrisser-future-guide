'use strict';

const fs = require('fs');
const path = require('path');

const ROOT=path.resolve(__dirname,'..');
const SOURCE_PATH=path.join(ROOT,'data/generated/hero-heart-fetter-job-effect-research.v1.json');
const POLICY_PATH=path.join(ROOT,'data/validation/hero-heart-fetter-presentation-policy.v1.json');
const HERO_DIR=path.join(ROOT,'data/generated/hero-detail/by-id');
function read(file){return JSON.parse(fs.readFileSync(file,'utf8'));}
function outputPath(argv){const i=argv.indexOf('--output');if(i===-1||!argv[i+1])throw new Error('Usage: node scripts/build-hero-heart-fetter-presentation-research-v1.cjs --output <path>');return path.resolve(argv[i+1]);}
function sourceKey(row){return [row.heroId,row.heartFetterLevel,row.skill.skillId,row.buff.buffId,row.condition.conditionParamJobId].join(':');}
function policyKey(row){return [row.heroId,row.heartFetterLevel,row.skillId,row.buffId,row.conditionParamJobId].join(':');}
function mapByKey(rows){return new Map(rows.map((row)=>[policyKey(row),row]));}
function heroJobs(heroId){const shard=read(path.join(HERO_DIR,`${heroId}.json`));const jobs=new Map();for(const c of shard?.normal?.jobTree?.connections||[]){if(Number.isInteger(c?.jobId))jobs.set(c.jobId,{jobId:c.jobId,nameCn:c?.job?.nameCn??null,rank:Number.isInteger(c?.job?.rank)?c.job.rank:null,source:'normal.jobTree.connections'});}if(shard?.sp?.job?.jobId)jobs.set(shard.sp.job.jobId,{jobId:shard.sp.job.jobId,nameCn:shard.sp.job.nameCn??null,rank:Number.isInteger(shard.sp.job.rank)?shard.sp.job.rank:null,source:'sp.job'});return jobs;}

function main(){
  const source=read(SOURCE_PATH),policy=read(POLICY_PATH);if(source.status!=='RESEARCH_PROJECTION'||source.productionConsumerAllowed!==false)throw new Error('job-effect research projection boundary changed');if(policy.status!=='RESEARCH_POLICY'||policy.validationBoundary?.productionFrontendConsumption!==false)throw new Error('presentation policy boundary changed');
  const overrides=mapByKey(policy.policy.explicitOverrides),exclusions=mapByKey(policy.policy.exclusionSetRows),conflicts=mapByKey(policy.policy.sourceConflictResolutions);const cache=new Map();const effects=[];const counts={VALIDATED_DEFAULT:0,EXPLICIT_OVERRIDE:0,EXCLUSION_SET_MEMBER:0,SOURCE_CONFLICT_RESOLUTION:0};
  for(const row of source.effects||[]){const k=sourceKey(row);const mapping=overrides.get(k)?{mode:'EXPLICIT_OVERRIDE',row:overrides.get(k)}:exclusions.get(k)?{mode:'EXCLUSION_SET_MEMBER',row:exclusions.get(k)}:conflicts.get(k)?{mode:'SOURCE_CONFLICT_RESOLUTION',row:conflicts.get(k)}:{mode:'VALIDATED_DEFAULT',row:null};const presentationJobId=mapping.row?mapping.row.presentationJobId:row.condition.conditionParamJobId;let jobs=cache.get(row.heroId);if(!jobs){jobs=heroJobs(row.heroId);cache.set(row.heroId,jobs);}const presentationJob=jobs.get(presentationJobId);if(!presentationJob)throw new Error(`Hero ${row.heroId}: unresolved presentation Job ${presentationJobId}`);counts[mapping.mode]++;effects.push({heroId:row.heroId,identity:row.identity,heartFetterLevel:row.heartFetterLevel,mappingMode:mapping.mode,conditionJobReference:row.job,presentationJob,resolutionEvidence:mapping.mode==='SOURCE_CONFLICT_RESOLUTION'?mapping.row.resolutionEvidence:null,excludedJobId:mapping.mode==='EXCLUSION_SET_MEMBER'?mapping.row.excludedJobId:null,skill:row.skill,buff:row.buff,condition:row.condition,sourceProvenance:row.sourceProvenance});}
  if(counts.VALIDATED_DEFAULT!==1142||counts.EXPLICIT_OVERRIDE!==6||counts.EXCLUSION_SET_MEMBER!==8||counts.SOURCE_CONFLICT_RESOLUTION!==2||effects.length!==1158)throw new Error(`unexpected presentation counts ${JSON.stringify(counts)} rows=${effects.length}`);
  const output={schemaVersion:1,stage:'hero-heart-fetter-presentation-research-v1',status:'RESEARCH_PROJECTION',semanticAuthority:false,productionConsumerAllowed:false,sourceProjection:'data/generated/hero-heart-fetter-job-effect-research.v1.json',presentationPolicy:'data/validation/hero-heart-fetter-presentation-policy.v1.json',heroPopulationCount:new Set(effects.map((row)=>row.heroId)).size,effectRowCount:effects.length,defaultRuleRowCount:counts.VALIDATED_DEFAULT,overrideRowCount:counts.EXPLICIT_OVERRIDE,exclusionSetRowCount:counts.EXCLUSION_SET_MEMBER,sourceConflictResolutionRowCount:counts.SOURCE_CONFLICT_RESOLUTION,unresolvedPresentationRowCount:0,levels:[4,7],notes:['presentationJob is research-only and does not assign generic ConditionType=7 runtime meaning.','Six exact Skill-vs-ConditionParam differences use explicit ID-keyed overrides: Ainz 4 and Kagura 2.','Eight Matthew rows are explicit exclusion-set members.','Two Narm rows use explicit source-conflict resolution to Job 622 based on frozen Job, ConditionParam, Buff evidence and separately recorded external cross-validation.','conditionJobReference remains preserved separately.','This artifact must not be consumed by production frontend code without a separate production handoff contract.'],effects};fs.writeFileSync(outputPath(process.argv.slice(2)),`${JSON.stringify(output,null,2)}\n`);
}
main();
