name: F3 Extract Soldier ID 300 Record

on:
  workflow_dispatch:
  pull_request:
    branches:
      - main
    paths:
      - .github/workflows/f3-extract-leon-hero-record.yml

permissions:
  contents: read

jobs:
  extract:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Node 22
        uses: actions/setup-node@v4
        with:
          node-version: 22

      - name: Extract Soldier ID 300 record
        env:
          GITHUB_TOKEN: ${{ github.token }}
        run: |
          set -euo pipefail
          node <<'NODE'
          const fs = require('fs');
          const {
            resolveConfigDataFile,
          } = require('./scripts/configdata-source-pack-maintenance-root.cjs');

          const soldierPath = resolveConfigDataFile('ConfigDataSoldierInfo.json');
          const soldiers = JSON.parse(fs.readFileSync(soldierPath, 'utf8'));
          const record = soldiers[54];

          if (!record) {
            throw new Error('recordIndex 54 missing');
          }

          if (record.ID !== 300) {
            throw new Error(`identity mismatch: expected ID 300, got ${record.ID}`);
          }

          console.log('=== F3_SOLDIER_300_RECORD_START ===');
          console.log(JSON.stringify(record, null, 2));
          console.log('=== F3_SOLDIER_300_RECORD_END ===');
          NODE

      - name: Verify repository unchanged
        run: |
          set -euo pipefail
          git diff --exit-code
          test -z "$(git status --porcelain)"
