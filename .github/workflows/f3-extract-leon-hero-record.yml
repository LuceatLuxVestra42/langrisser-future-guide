name: F3 Extract Leon Hero Record

on:
  workflow_dispatch:
  pull_request:
    branches:
      - main
    paths:
      - .github/workflows/f3-extract-leon-hero-record.yml

permissions:
  contents: read

jobs:
  extract:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Node 22
        uses: actions/setup-node@v4
        with:
          node-version: 22

      - name: Extract Hero ID 6 record
        env:
          GITHUB_TOKEN: ${{ github.token }}
        run: |
          set -euo pipefail
          node <<'NODE'
          const fs = require('fs');
          const {
            resolveConfigDataFile,
          } = require('./scripts/configdata-source-pack-maintenance-root.cjs');

          const heroPath = resolveConfigDataFile('ConfigDataHeroInfo.json');
          const heroes = JSON.parse(fs.readFileSync(heroPath, 'utf8'));
          const record = heroes[5];

          if (!record) {
            throw new Error('recordIndex 5 missing');
          }

          if (record.ID !== 6) {
            throw new Error(`identity mismatch: expected ID 6, got ${record.ID}`);
          }

          console.log('=== F3_LEON_RECORD_START ===');
          console.log(JSON.stringify(record, null, 2));
          console.log('=== F3_LEON_RECORD_END ===');
          NODE

      - name: Verify repository unchanged
        run: |
          set -euo pipefail
          git diff --exit-code
          test -z "$(git status --porcelain)"
