name: Asset Hygiene Stage 2 Temporary Producer

on:
  push:
    branches:
      - work/asset-intake-hygiene-stage2-reference-map-v1
  workflow_dispatch:

permissions:
  contents: read

jobs:
  explicit-producer:
    if: >-
      (github.event_name == 'workflow_dispatch') ||
      (github.event_name == 'push' && contains(github.event.head_commit.message, '[asset-hygiene-stage2-produce]'))
    runs-on: ubuntu-24.04
    permissions:
      contents: write
    steps:
      - uses: actions/checkout@v5
        with:
          fetch-depth: 0
          ref: ${{ github.ref_name }}
      - uses: actions/setup-node@v7
        with:
          node-version: 22
      - name: Generate Stage 2 reference map
        run: node tools/asset-intake/cli/run-hygiene-stage2-v1.mjs
      - name: Freeze generated Stage 2 artifacts
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add \
            tools/asset-intake/hygiene/generated/asset-hygiene-reference-map.v1.json \
            tools/asset-intake/hygiene/generated/asset-hygiene-unresolved-reference.v1.json \
            data/validation/asset-intake-hygiene-stage2-reference-crosscheck-summary.v1.json \
            docs/checkpoints/asset-intake-hygiene-stage2-reference-crosscheck.md
          git commit -m "Freeze Asset Hygiene Stage 2 reference map"
      - name: Validate committed Stage 2 reference map
        run: node tools/asset-intake/cli/validate-hygiene-stage2-v1.mjs
      - name: Push frozen Stage 2 artifacts
        run: git push origin HEAD:${{ github.ref_name }}
