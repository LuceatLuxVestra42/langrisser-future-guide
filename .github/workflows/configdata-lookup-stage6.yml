name: ConfigData Lookup Stage 6

on:
  workflow_dispatch:
  push:
    branches:
      - main
      - work/configdata-lookup-stage6-incremental
    paths:
      - 'data/configdata/ConfigDataHeroInfo.json'
      - 'data/configdata/ConfigDataJobConnectionInfo.json'
      - 'data/configdata/ConfigDataJobInfo.json'
      - 'data/configdata/ConfigDataSkillInfo.json'
      - 'data/configdata/ConfigDataSoldierInfo.json'
      - 'data/configdata/ConfigDataTrainingTechInfo.json'
      - 'data/configdata/ConfigDataTrainingTechLevelInfo.json'
      - 'data/configdata/ConfigDataEquipmentInfo.json'
      - 'data/generated/hero-soldier-by-hero.v1.json'
      - 'data/generated/hero-soldier-by-soldier.v1.json'
      - 'data/generated/hero-exclusive-equipment-by-hero.v1.json'
      - 'data/generated/hero-exclusive-equipment-by-equipment.v1.json'
      - 'data/contracts/configdata-lookup-stage*.json'
      - 'data/contracts/configdata-source-pack-contract.v1.json'
      - 'data/contracts/project-tooling-configdata-lookup-*.json'
      - 'data/index/hero.v1.json'
      - 'data/index/soldier.v1.json'
      - 'data/index/skill.v1.json'
      - 'data/index/equipment.v1.json'
      - 'data/index/forward/**'
      - 'data/index/reverse/**'
      - 'data/index/overlay/**'
      - 'data/index/configdata-lookup-dependency-manifest.v1.json'
      - 'data/validation/configdata-lookup-stage*-summary.v1.json'
      - 'scripts/lib/configdata-lookup-stage*.mjs'
      - 'scripts/build-configdata-lookup-stage*.mjs'
      - 'scripts/validate-configdata-lookup-stage*.mjs'
      - 'scripts/configdata-lookup*.mjs'
      - 'scripts/hydrate-configdata-source-pack-v1.mjs'
      - 'tools/configdata-lookup/**'
      - 'package.json'
      - '.github/workflows/configdata-lookup-stage6.yml'
  pull_request:
    branches:
      - main
    paths:
      - 'data/configdata/ConfigDataHeroInfo.json'
      - 'data/configdata/ConfigDataJobConnectionInfo.json'
      - 'data/configdata/ConfigDataJobInfo.json'
      - 'data/configdata/ConfigDataSkillInfo.json'
      - 'data/configdata/ConfigDataSoldierInfo.json'
      - 'data/configdata/ConfigDataTrainingTechInfo.json'
      - 'data/configdata/ConfigDataTrainingTechLevelInfo.json'
      - 'data/configdata/ConfigDataEquipmentInfo.json'
      - 'data/generated/hero-soldier-by-hero.v1.json'
      - 'data/generated/hero-soldier-by-soldier.v1.json'
      - 'data/generated/hero-exclusive-equipment-by-hero.v1.json'
      - 'data/generated/hero-exclusive-equipment-by-equipment.v1.json'
      - 'data/contracts/configdata-lookup-stage*.json'
      - 'data/contracts/configdata-source-pack-contract.v1.json'
      - 'data/contracts/project-tooling-configdata-lookup-*.json'
      - 'data/index/hero.v1.json'
      - 'data/index/soldier.v1.json'
      - 'data/index/skill.v1.json'
      - 'data/index/equipment.v1.json'
      - 'data/index/forward/**'
      - 'data/index/reverse/**'
      - 'data/index/overlay/**'
      - 'data/index/configdata-lookup-dependency-manifest.v1.json'
      - 'data/validation/configdata-lookup-stage*-summary.v1.json'
      - 'scripts/lib/configdata-lookup-stage*.mjs'
      - 'scripts/build-configdata-lookup-stage*.mjs'
      - 'scripts/validate-configdata-lookup-stage*.mjs'
      - 'scripts/configdata-lookup*.mjs'
      - 'scripts/hydrate-configdata-source-pack-v1.mjs'
      - 'tools/configdata-lookup/**'
      - 'package.json'
      - '.github/workflows/configdata-lookup-stage6.yml'

permissions:
  contents: read

jobs:
  validation-only:
    runs-on: ubuntu-latest
    env:
      CONFIGDATA_SOURCE_ROOT: ${{ runner.temp }}/configdata-lookup-stage6-source
    steps:
      - name: Checkout
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Setup Node
        uses: actions/setup-node@v4
        with:
          node-version: '22'

      - name: Hydrate and verify pinned ConfigData source pack outside repository
        env:
          GITHUB_TOKEN: ${{ github.token }}
        shell: bash
        run: |
          set -euo pipefail
          rm -rf "$CONFIGDATA_SOURCE_ROOT"
          node scripts/hydrate-configdata-source-pack-v1.mjs --target-dir "$CONFIGDATA_SOURCE_ROOT"
          count="$(find "$CONFIGDATA_SOURCE_ROOT/data/configdata" -maxdepth 1 -type f -name '*.json' | wc -l | tr -d ' ')"
          test "$count" = "753"
          case "$CONFIGDATA_SOURCE_ROOT/" in
            "$GITHUB_WORKSPACE/"*) echo 'external ConfigData source root must stay outside repository' >&2; exit 1 ;;
          esac

      - name: Make tracked ConfigData unavailable during Stage 6 validation
        shell: bash
        run: |
          set -euo pipefail
          tracked_raw="$GITHUB_WORKSPACE/data/configdata"
          hidden_raw="$RUNNER_TEMP/configdata-stage6-tracked-hidden"
          rm -rf "$hidden_raw"
          if [ -d "$tracked_raw" ]; then
            mv "$tracked_raw" "$hidden_raw"
          fi
          test ! -e "$tracked_raw"

      - name: Validate Stage 5 predecessor
        run: npm run validate:configdata-lookup-stage5

      - name: Validate Stage 6 committed materialization
        run: node tools/configdata-lookup/test/configdata-lookup-stage6-current-validator.mjs

      - name: Require clean stale plan through current source-root adapter
        run: node tools/configdata-lookup/cli/check-freshness.mjs

      - name: Restore tracked ConfigData when present in this revision
        if: always()
        shell: bash
        run: |
          set -euo pipefail
          tracked_raw="$GITHUB_WORKSPACE/data/configdata"
          hidden_raw="$RUNNER_TEMP/configdata-stage6-tracked-hidden"
          if [ -d "$hidden_raw" ]; then
            test ! -e "$tracked_raw"
            mv "$hidden_raw" "$tracked_raw"
          fi

      - name: Require read-only tracked state
        shell: bash
        run: git diff --exit-code
