name: ConfigData Lookup Stage 8 (Retired)

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  retired:
    runs-on: ubuntu-latest
    steps:
      - name: Report retired orchestration
        run: |
          echo 'This historical ConfigData Lookup orchestration was retired by CLR7.'
          echo 'Frozen contracts, validators, summaries, and Git history remain the historical evidence.'
          echo 'Current impact and validation authority is owned by independent Project tooling.'
