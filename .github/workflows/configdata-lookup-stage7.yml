name: ConfigData Lookup Stage 7 (Retired)

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  retired:
    runs-on: ubuntu-latest
    steps:
      - name: Report retired orchestration
        run: |
          echo 'This historical ConfigData Lookup orchestration was retired by CLR7.'
          echo 'Frozen contracts, validators, summaries, and Git history remain the historical evidence.'
          echo 'Current lookup authority is owned by the NEW ConfigData Lookup namespace and Project Check.'
