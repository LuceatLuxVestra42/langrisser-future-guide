name: Soldier Stage 5-5 Hero Membership

on:
  push:
    branches:
      - main
      - 'work/**'
    paths:
      - 'scripts/finalize-soldier-stage5-5-heroes.cjs'
      - 'data/contracts/soldier-detail-stage5-1-contract.v1.json'
      - 'data/generated/soldier-detail-stage5-4.v1.json'
      - 'data/validation/soldier-stage5-4-training.v1.json'
      - 'data/generated/hero-soldier-by-soldier.v1.json'
      - 'data/validation/hero-soldier-relation-validation.v1.json'
      - 'data/generated/soldier-detail-stage5-5.v1.json'
      - 'data/validation/soldier-stage5-5-heroes.v1.json'
      - '.github/workflows/soldier-stage5-5-heroes.yml'
  pull_request:
    paths:
      - 'scripts/finalize-soldier-stage5-5-heroes.cjs'
      - 'data/contracts/soldier-detail-stage5-1-contract.v1.json'
      - 'data/generated/soldier-detail-stage5-4.v1.json'
      - 'data/validation/soldier-stage5-4-training.v1.json'
      - 'data/generated/hero-soldier-by-soldier.v1.json'
      - 'data/validation/hero-soldier-relation-validation.v1.json'
      - 'data/generated/soldier-detail-stage5-5.v1.json'
      - 'data/validation/soldier-stage5-5-heroes.v1.json'
      - '.github/workflows/soldier-stage5-5-heroes.yml'

permissions:
  contents: write

jobs:
  generate-validate:
    runs-on: ubuntu-24.04
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0
          ref: ${{ github.event_name == 'pull_request' && github.head_ref || github.ref_name }}

      - uses: actions/setup-node@v4
        with:
          node-version: 22

      - name: Generate and validate Soldier Stage 5-5 Hero membership
        run: node scripts/finalize-soldier-stage5-5-heroes.cjs

      - name: Upload Stage 5-5 artifacts
        uses: actions/upload-artifact@v4
        with:
          name: soldier-stage5-5-heroes
          path: |
            data/generated/soldier-detail-stage5-5.v1.json
            data/validation/soldier-stage5-5-heroes.v1.json

      - name: Freeze generated artifacts on work branch
        if: github.event_name == 'push' && github.ref != 'refs/heads/main'
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add \
            data/generated/soldier-detail-stage5-5.v1.json \
            data/validation/soldier-stage5-5-heroes.v1.json
          if git diff --cached --quiet; then
            echo "Soldier Stage 5-5 artifacts already frozen."
          else
            git commit -m "freeze Soldier stage 5-5 Hero membership PASS"
            git push origin HEAD:${{ github.ref_name }}
          fi

      - name: Verify frozen artifacts on main or PR
        if: github.event_name != 'push' || github.ref == 'refs/heads/main'
        run: git diff --exit-code -- data/generated/soldier-detail-stage5-5.v1.json data/validation/soldier-stage5-5-heroes.v1.json

# refresh: name-fixes-final-20260829
