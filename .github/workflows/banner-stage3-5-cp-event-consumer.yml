name: Banner Stage 3-5 CP Event Consumer

on:
  push:
    paths:
      - '.github/workflows/banner-stage3-5-cp-event-consumer.yml'
      - 'scripts/finalize-banner-stage3-5-cp-event-consumer.mjs'
      - 'scripts/validate-banner-stage3-5-cp-event-consumer.mjs'
      - 'data/contracts/banner-stage3-5-cp-event-consumer.v1.json'
      - 'data/validation/banner-stage3-3-basic-table-summary.v1.json'
      - 'data/generated/banner-stage3-3-basic-table-consumer.v1.json'
      - 'data/generated/banner-cp-event-relations.v1.json'

permissions:
  contents: write

jobs:
  validate:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Materialize Banner Stage 3-5 CP Event consumer
        run: node scripts/finalize-banner-stage3-5-cp-event-consumer.mjs
      - name: Validate Banner Stage 3-5 CP Event consumer
        run: node scripts/validate-banner-stage3-5-cp-event-consumer.mjs
      - name: Persist Banner Stage 3-5 checkpoint
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add data/generated/banner-stage3-5-cp-event-consumer.v1.json data/validation/banner-stage3-5-cp-event-summary.v1.json
          if git diff --cached --quiet; then
            echo "No Banner Stage 3-5 checkpoint changes"
            exit 0
          fi
          git commit -m "Freeze Banner Stage 3-5 CP Event checkpoint"
          git push
