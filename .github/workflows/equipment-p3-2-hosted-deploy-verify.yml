name: Equipment P3-2 Hosted Deploy Verify (Retired Publisher)

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  retired:
    runs-on: ubuntu-latest
    steps:
      - name: Legacy publisher retired
        run: |
          echo "This legacy Hosted QA no longer publishes gh-pages."
          echo "Use .github/workflows/authoritative-pages-deploy.yml on main for production publication and hosted verification."
