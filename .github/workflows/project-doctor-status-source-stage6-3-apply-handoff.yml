name: Project Status Stage 6-3 Apply Handoff (Retired)

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  retired:
    runs-on: ubuntu-latest
    steps:
      - name: Explain retired writer transport
        run: |
          echo 'The legacy Project Doctor Status Source writer transport is retired.'
          echo 'Use the NEW Project Tooling R1 Status Source Writer on an explicit non-main work branch.'
          exit 1
