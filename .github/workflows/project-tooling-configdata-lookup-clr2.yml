name: Project Tooling ConfigData Lookup CLR2

on:
  pull_request:
    branches:
      - main
    types: [opened, synchronize, reopened, ready_for_review]
    paths:
      - 'tools/configdata-lookup/**'
      - 'data/contracts/project-tooling-project-check-configdata-lookup-clr2-*.json'
      - 'tools/project-check/contracts/owners.v1.json'
      - '.github/workflows/project-tooling-configdata-lookup-clr2.yml'
  workflow_dispatch:

permissions:
  contents: read

concurrency:
  group: project-tooling-configdata-lookup-clr2-${{ github.ref }}
  cancel-in-progress: true

jobs:
  clr2-namespace:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Node 22
        uses: actions/setup-node@v4
        with:
          node-version: '22'

      - name: Validate frozen Stage 5 predecessor
        run: node scripts/validate-configdata-lookup-stage5.mjs

      - name: Require clean Stage 6 freshness
        run: node scripts/configdata-lookup-stage6.mjs check

      - name: Run CLR2 namespace smoke
        run: node tools/configdata-lookup/test/clr2-namespace-smoke.mjs
