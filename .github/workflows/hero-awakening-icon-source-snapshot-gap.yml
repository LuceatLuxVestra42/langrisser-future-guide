name: Hero Awakening Icon Source Snapshot Gap

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  transition:
    runs-on: ubuntu-latest
    steps:
      - run: echo 'A6-7 workflow transition in progress; final matrix workflow follows on this branch.'
