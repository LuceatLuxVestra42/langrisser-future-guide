name: Equipment Stage 4-5 Display State Policy

on:
  workflow_dispatch:
  push:
    paths:
      - '.github/workflows/equipment-stage4-5-display-state-policy.yml'
      - 'scripts/validate-equipment-stage4-5-display-state-policy.mjs'
      - 'data/contracts/equipment-stage4-5-display-state-policy.v1.json'
      - 'src/routes/equipment.tsx'
      - 'src/routes/equipment_.exclusive.tsx'
      - 'src/routes/equipment_.$equipmentId.tsx'
      - 'data/contracts/equipment-stage4-0-frontend-consumer-contract.v1.json'
      - 'data/generated/equipment_stage3_3_general_list.json'
      - 'data/generated/equipment_stage3_5_exclusive_consumer.json'
      - 'data/validation/equipment-stage4-2-general-list-ui-summary.v1.json'
      - 'data/validation/equipment-stage4-3-general-detail-ui-summary.v1.json'
      - 'data/validation/equipment-stage4-4-exclusive-ui-ownership-summary.v1.json'
  pull_request:
    branches:
      - main
    paths:
      - '.github/workflows/equipment-stage4-5-display-state-policy.yml'
      - 'scripts/validate-equipment-stage4-5-display-state-policy.mjs'
      - 'data/contracts/equipment-stage4-5-display-state-policy.v1.json'
      - 'src/routes/equipment.tsx'
      - 'src/routes/equipment_.exclusive.tsx'
      - 'src/routes/equipment_.$equipmentId.tsx'
      - 'data/contracts/equipment-stage4-0-frontend-consumer-contract.v1.json'
      - 'data/generated/equipment_stage3_3_general_list.json'
      - 'data/generated/equipment_stage3_5_exclusive_consumer.json'
      - 'data/validation/equipment-stage4-2-general-list-ui-summary.v1.json'
      - 'data/validation/equipment-stage4-3-general-detail-ui-summary.v1.json'
      - 'data/validation/equipment-stage4-4-exclusive-ui-ownership-summary.v1.json'

permissions:
  contents: read

jobs:
  reject-main-dispatch:
    if: github.event_name == 'workflow_dispatch' && github.ref == 'refs/heads/main'
    runs-on: ubuntu-latest
    permissions:
      contents: read
    steps:
      - name: Reject protected main as explicit producer target
        run: |
          echo 'Explicit Equipment Stage 4-5 producer runs must target a non-main branch. Freeze there, then open a normal PR.'
          exit 1

  validate:
    if: github.event_name != 'workflow_dispatch'
    runs-on: ubuntu-latest
    permissions:
      contents: read
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0
      - uses: oven-sh/setup-bun@v2
      - name: Install dependencies
        run: bun install --frozen-lockfile
      - name: Validate Equipment Stage 4-5 display/state policy
        run: node scripts/validate-equipment-stage4-5-display-state-policy.mjs
      - name: Verify frozen Stage 4-5 checkpoint
        shell: bash
        run: |
          set -euo pipefail
          target='data/validation/equipment-stage4-5-display-state-policy-summary.v1.json'
          if ! git diff --exit-code -- "$target"; then
            echo 'Equipment Stage 4-5 frozen checkpoint is stale. Run this workflow explicitly on a non-main producer branch and open a normal PR with the refreshed checkpoint.'
            exit 1
          fi
          echo 'Equipment Stage 4-5 frozen checkpoint matches validation output.'

  explicit-producer:
    if: github.event_name == 'workflow_dispatch' && github.ref != 'refs/heads/main'
    runs-on: ubuntu-latest
    permissions:
      contents: write
    steps:
      - name: Checkout explicit producer target
        uses: actions/checkout@v4
        with:
          fetch-depth: 0
          ref: ${{ github.ref_name }}
      - uses: oven-sh/setup-bun@v2
      - name: Install dependencies
        run: bun install --frozen-lockfile
      - name: Validate Equipment Stage 4-5 display/state policy
        run: node scripts/validate-equipment-stage4-5-display-state-policy.mjs
      - name: Freeze Stage 4-5 checkpoint on explicit producer branch
        shell: bash
        run: |
          set -euo pipefail
          OUTPUT='data/validation/equipment-stage4-5-display-state-policy-summary.v1.json'
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add "$OUTPUT"
          if git diff --cached --quiet; then
            echo 'Equipment Stage 4-5 frozen checkpoint is already current.'
          else
            git commit -m 'Freeze Equipment Stage 4-5 display state policy checkpoint'
            git push origin HEAD:${{ github.ref_name }}
          fi
