name: Equipment Image Stage 2 Sprite Parity Diagnostic v2

on:
  push:
    branches:
      - work/equipment-image-stage2-apk-final373-v2
    paths:
      - scripts/diagnose-equipment-image-stage2-sprite-parity-v2.py
      - .github/workflows/equipment-image-stage2-sprite-parity-diagnostic-v2.yml
  workflow_dispatch:

permissions:
  contents: read

jobs:
  diagnose:
    runs-on: ubuntu-latest
    timeout-minutes: 30
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.12'

      - name: Install dependencies
        run: python -m pip install --disable-pip-version-check UnityPy Pillow

      - name: Compare Sprite exports against frozen representatives
        run: python scripts/diagnose-equipment-image-stage2-sprite-parity-v2.py
